# 函数定义与使用

```python
# 定义函数
def greet(name, greeting="Hello"):
    """返回问候语"""
    return f"{greeting}, {name}!"

# 使用函数
result = greet("World")
print(result)  # Hello, World!

# Lambda函数（匿名函数）
square = lambda x: x ** 2
```
