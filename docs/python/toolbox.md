# Python 核心工具箱

## 1. 数据科学与机器学习

```python
# 数据分析三剑客
import numpy as np          # 数值计算
import pandas as pd         # 数据处理
import matplotlib.pyplot as plt  # 数据可视化

# 机器学习
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier

# 深度学习
import tensorflow as tf
import torch
```

## 2. Web开发

```python
# 后端框架
from flask import Flask     # 轻量级框架
from django.http import HttpResponse  # 全功能框架

# API开发
from fastapi import FastAPI
import requests

# 异步支持
import asyncio
import aiohttp
```

## 3. 自动化与脚本

```python
# 系统操作
import os
import sys
import shutil

# 文件处理
import json
import csv
import pickle

# 定时任务
import schedule
import time
```

## 4. 网络爬虫

```python
import requests            # HTTP请求
from bs4 import BeautifulSoup  # HTML解析
import scrapy              # 爬虫框架
import selenium            # 浏览器自动化
```

## 5. GUI开发

```python
import tkinter as tk       # 内置GUI库
import PyQt5               # 跨平台GUI
import wxPython            # 另一个GUI框架
```
